{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debugging with Chrome",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:4200",
            "sourceMaps": true,
            "webRoot": "${workspaceRoot}"
        }
    ]
}